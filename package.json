{
  "version": "1.0.0",
  "description": "A simple CAP project.",
  "repository": "https://github.com/dhmarzano/sap_cap.git",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@sap/cds": "^7.9.2",
    "better-sqlite3": "^10.0.0",
    "express": "^4.19.2",
    "sqlite": "^5.1.1"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^1.7.1"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "sqlite",
        "credentials": {
          "database": "db.sqlite"
        }
      }
    }
  },
  "scripts": {
    "start": "cds-serve"
  }
}